{% extends "@GlynnTemplate/admin.html.twig" %}

{% block title %}
    Puzzle - {{ parent() }}
{% endblock %}

{% block body %}
    <div class="container">
        <h1>Daily photos</h1>
        {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit">aa</button>
        {{ form_end(form) }}

        {% for item in daily %}
            {{ item.dateDaily }}
        {% else %}
            <div class="col-12">
                No items found
            </div>
        {% endfor %}
    </div>
{% endblock %}
